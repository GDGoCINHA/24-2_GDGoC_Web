# 작업 공간 — 커밋되지 않는 것이 요점이다.
#
# 조사 메모·중간 산출물·대조 리포트처럼 그 과제에서만 쓰는 것의 기본 목적지다.
# 레포에 남는 건 작업이 끝난 뒤에 제대로 쓴 최종본뿐이다 (rules/artifact-lifecycle.md).
work/

# 회수 보관소 — 끝난 과제에서 물러난 산출물.
# 삭제가 아니라 이동이라 되돌릴 수 있고, 로드되지 않으므로 컨텍스트 비용이 0이다.
attic/

# 이 PC 전용 설정
settings.local.json
